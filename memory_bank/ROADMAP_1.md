# Development Roadmap 1

This document outlines the planned development phases for the `bot-svc` project.

## Phase 1: Initial Setup (Completed)

- [x] Installed project dependencies (`aiogram`, `motor`, `pydantic`, `pydantic-settings`) using `uv`.
- [x] Configured VSCode settings (`settings.json`) for formatting (`ruff`) and type checking (`pyright`).
- [x] Configured `pyproject.toml` with `ruff` and `pyright` settings.
- [x] Created basic project directory structure (`src/`).
- [x] Implemented a minimal Telegram bot (`src/main.py`) using `aiogram` and `pydantic-settings`.
- [x] Configured VSCode debugging (`launch.json`).
- [x] Prepared environment variable management (`.env.example`, `.gitignore`).
- [x] Created basic README documentation (`README.md`).
- [x] Added basic test structure and initial test (`tests/test_bot.py`).
